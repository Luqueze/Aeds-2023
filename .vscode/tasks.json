{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compilar",
            "type": "shell",
            "command": "gcc -c ${file} -o ${fileDirname}/${fileBasenameNoExtension}.o",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
