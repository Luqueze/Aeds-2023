#include<stdio.h>

    int main(){

        int idade;
        float h;

        printf("insira sua idade: ");
        if(scanf("%d", &idade) < 1 || idade < 6){
            printf("Não apto");
        }

        printf("Insira sua altura: ");
        if(scanf("%d"))
    } //code in progress